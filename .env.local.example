# The Reader tab calls Claude to interpret a spread. Everything else in the
# app runs without this. Get a key from https://console.anthropic.com/ and
# copy this file to .env.local — Next reads it at startup, so restart
# `npm run dev` after adding it.
ANTHROPIC_API_KEY=sk-ant-...
