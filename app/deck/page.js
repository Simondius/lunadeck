export default function DeckPage() {
  return (
    <main className="shell">
      <span className="unit-eyebrow">Deck</span>
      <h1 className="unit-title">Your collection</h1>
      <p className="unit-intro">
        Every card you have met, and the ones still ahead. Designed, not built
        yet — it lands with the collection screens.
      </p>
    </main>
  );
}
